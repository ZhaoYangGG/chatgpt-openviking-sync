# 隐私说明

## 数据流

V2 观察 ChatGPT 自然详情与历史分页，解析接纳的 User/最终 Assistant 纯文本、源身份和时间，保存在扩展 IndexedDB。图文 User 消息仅提取字符串文字部分，记录“非文本未同步”标记，不保存或下载 asset pointer 所指资源。开启同步后发送到使用者配置的 OpenViking；该服务可能归档、摘要与提取长期记忆。

原始响应处理期间在内存中存在，不以完整响应形式持久保存。源时间数字文本和接纳的消息内容会持久保存。首次详情必须显式允许非临时、非 do-not-remember 采集。省略隐私字段的历史分页只可使用同一文档、会话和目的地的已验证上下文；上下文保存在可信 session storage，显式隐私限制会撤销它。未知内容类型不被默默视为成功。这些过滤不认证被攻陷页面的隐私字段。

无项目运营方的内容收集/广告/遥测端点。浏览器与 ChatGPT 自身行为不在此声明内。

## 本地与远端控制

默认不开启上传；采集可能先形成本地记录。停用扩展可停止后续采集，不自动删除已有内容。清理数据/卸载可能丢失队列；本地删除不会删除远端 Session 或记忆。

凭据在扩展可信上下文保存，但存储不由本项目加密。保护本机和 profile。不同目的地/Key/Agent/命名空间隔离本地队列，不自动转发旧数据。原 preview 队列不迁入生产上传。

## 发布与反馈

公开仓库只包含源码、合成测试、脱敏结论。不要公开真实聊天、会话 ID/标题/时间、实验导出、桌面采集、浏览器配置、凭据、姓名邮箱或本机路径。

即使“无正文诊断”也可能含隐私。反馈优先用合成复现，检查截图侧边栏、头像、地址栏和错误提示。GitHub 平台仍会展示仓库所属账号；源码脱敏不等于平台账号匿名化。
