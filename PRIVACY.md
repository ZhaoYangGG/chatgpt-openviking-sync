# 隐私说明

## 数据流向

- V1：页面 User/Assistant 文本、来源 ID、时间及轮次进入扩展本地队列；启用后发送至使用者配置的 OpenViking。服务端可能继续归档、摘要与提取记忆，受该部署的策略约束。
- V2：接受的正文、身份、时间及修订保存在扩展 IndexedDB，无上传实现。原始响应在处理期间进入内存，不作为完整响应持久保存。
- P0：有限事件诊断环；导出即使无正文，也可能包含标题、会话 ID、精确时间与模型。

项目无运营方收集端点。ChatGPT 与浏览器自身的网络行为不在此声明涵盖范围内。

## 本地控制

V1 配置含使用者填写的服务地址和凭据；本地存储不是加密保险箱。V2 加载即观察，停用停止后续采集，但不自动清理历史。

卸载/清理扩展数据可能丢失未上传队列，应先评估备份。数据不自动跨浏览器 profile 同步；V2 本地队列不是远端备份。删除本地数据不会删除已上传 Session 或远端记忆。

## 公开反馈

公开源码只包含实现、合成测试和脱敏文档。真实聊天、会话标识、样本时间、服务实验结果、桌面采集、浏览器配置、API Key、本机路径不应进入仓库或包。

反馈前检查文字和截图：标题、侧边栏、账户头像、地址栏、服务地址、Token、邮件和本机路径都可能暴露个人信息。优先合成复现，不依赖“没有正文就没有隐私”的判断。
