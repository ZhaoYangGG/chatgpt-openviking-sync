{
  "name": "chatgpt-openviking-webextension",
  "version": "0.2.2",
  "private": true,
  "description": "Chrome extension for observed ChatGPT conversation sync with source timestamps and best-effort deduplication.",
  "scripts": {
    "test": "node --test test/*.test.js",
    "validate": "node scripts/validate-extension.mjs",
    "build:v2-preview": "node scripts/build-v2-preview.mjs",
    "build:v2": "node scripts/build-v2.mjs",
    "package:release": "node scripts/package-release.mjs",
    "test:release": "node --test scripts/release.test.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "devDependencies": {
    "fake-indexeddb": "^6.2.5"
  }
}
