# 贡献指南

欢迎帮助项目提高采集正确性、安全性和恢复能力。当前优先可靠性，而非扩大内容类型或浏览器范围。

## 本地开发

Node.js 20+：

```bash
npm ci --ignore-scripts
npm test
npm run validate
npm run build:v2-preview
```

在 Chrome 加载 V2 构建目录验证。自动测试通过不是实机验收；fake-indexeddb 不能作为 Chrome 进程重启证明。

## 提交要求

1. 独立分支，解释问题、行为变化和边界。
2. 添加合成回归测试，不提交真实响应、个人会话 ID 或时间。
3. 持久化变更考虑事务、升级、崩溃恢复和未发送数据保护。
4. 上传变更说明写前/写后确认和结果未知恢复；本地锁不是远端原子幂等。
5. 页面桥保留原 fetch 行为，不主动请求历史，不接触登录凭据。
6. 文档明确区分已实现、已实测和计划中。

不要提交依赖、构建缓存、诊断导出、私人研究或浏览器存储。发布前执行 `npm run package:release` 并人工审查白名单与扫描结果。

## 真实服务实验

普通测试使用模拟请求和合成数据，不需要 Key。真实服务测试需单独授权、隔离 Session，并确认不会进入生产记忆；无法证明隔离时停止，不自动删除已有数据。

详见 [测试说明](docs/public/TESTING.md) 和 [安全说明](SECURITY.md)。提交贡献表示你有权按 MIT 许可提供内容；不要求将真实姓名或个人邮箱写进源码。
