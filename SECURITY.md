# 安全说明

V2 0.2.1 已包含上传功能，但默认关闭，需要使用者确认信任边界并停止旧上传器。本项目没有生产级安全或内容真实性保证。

## 页面转发防护

完整 V2 不接受旧的未签名 V2_CAPTURE。后台生成每文档随机密钥，通过 Chrome scripting 参数注入闭包，转发包含 HMAC、channel 和递增序号。后台核验不可由页面伪造的 sender documentId/tabId、签名及重放序号。密钥不放进 DOM、页面消息或持久配置；session/local storage 限制为可信扩展上下文。

这解决普通未签名、正文篡改、跨文档挪用和重放消息直接入队的问题；有对应合成回归测试。

**这不是 ChatGPT 服务端签名。** MAIN world 仍与页面共享环境。如果页面基础 API 在安装前被篡改、页面已被攻陷或其他扩展干预，采集内容仍可能不可信。不能把 HMAC 解释为“彻底防御恶意页面”。V2 设置要求明确知悉这一边界。旧只读 preview/P0 桥不具备此保护，不进入完整 V2 manifest。

## 上传与配置

OpenViking Key 只供可信扩展页面和后台使用，不注入页面。对服务请求拒绝重定向、省略浏览器凭据；远端要求 HTTPS。扩展本地存储并非加密保险箱，应保护本机/profile。

更换目标、Key、Agent 或命名空间会隔离队列。旧预览队列不自动升级为可信生产队列。跨扩展 V1 无法自动禁用，必须由使用者停止；不要在多上传器同时运行时假设互斥已生效。

写入结果未知保留 pending，恢复时先回读。不自动删除远端数据，日志来源不明时停写目的会话。跨设备没有服务端唯一约束，不能保证零重复。

## 报告与测试

只用合成数据反馈，避免凭据、真实聊天、ID、标题和私人截图。若 GitHub 提供 Private Vulnerability Reporting，可通过 Security 私下报告；否则开不含敏感细节的 Issue 请求协调。

服务诊断页会创建/追加合成数据，不自动销毁，普通测试不运行它。无法确认实验不会影响生产记忆时应停止。无固定响应 SLA 或漏洞奖励承诺。
