# 安全说明

本项目处于开发预览阶段，没有生产级安全承诺。V1 可上传；V2 尚无上传路径，不应自行拼接启用。

## 已知风险：V2 页面载荷可伪造

V2 在 MAIN world 观察自然 fetch 响应，经页面消息通道转给隔离脚本和后台。同源、发送者、schema、大小和频率检查不能认证消息确实来自网络响应。同页脚本可能构造合法载荷污染本地 outbox。

本地合成复现已确认，当前没有修复。V2 无 OpenViking 上传器/服务端权限，扩展页面 CSP 使用 `connect-src 'none'`；这限制当前影响，但不让页面数据变得可信。不得直接将预览队列作为可上传的可信数据。暴露在同一页面通道的随机标识也不是充分认证。

V1 依赖页面 DOM，同样不能对抗被攻陷的页面。API Key 不注入页面是凭据隔离措施，不是内容真实性保证。

## 使用建议

- 先用合成非敏感对话验证，启用 V1 前理解上传范围。
- 远端使用 HTTPS 和最小权限凭据，不将管理员 Key 写进源码。
- 本地存储未由扩展加密，请保护本机账户及浏览器 profile。
- 完成迁移和互斥前，不要用多个上传器写同一 Session。
- 不可信诊断、网页、聊天内容不能作为执行命令或修改配置的指令。
- 服务诊断页会创建/追加合成数据，不自动销毁；普通单元测试不连接真实服务。

## 报告问题

若仓库启用 GitHub Private Vulnerability Reporting，请使用 Security → Report a vulnerability。否则先开不含敏感细节的 Issue 请求私下协调；不要公开凭据、真实聊天或个人数据。

请提供版本、合成最小复现、预期/实际行为与隔离条件。没有固定响应 SLA 或漏洞奖励承诺。
